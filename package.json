{
  "name": "@dank-inc/use-get",
  "version": "0.3.3",
  "author": "Elijah Lucian",
  "description": "A few typscript react hook to get you that sweet sweet data from an endpoint.",
  "repository": {
    "url": "https://github.com/dank-inc/use-get"
  },
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "license": "ISC",
  "keywords": [
    "react",
    "api",
    "promise",
    "hook",
    "async",
    "loading state",
    "convenient"
  ],
  "scripts": {
    "lint": "tsc --noEmit",
    "audit": "npm audit --audit-level=\"moderate\"",
    "compile": "rm -rf dist && tsc",
    "deploy": "npm run compile && npm publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "peerDependencies": {
    "react": "^17.0.2"
  },
  "devDependencies": {
    "@types/react": "^17.0.3",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.3"
  }
}
