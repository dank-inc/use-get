{
  "name": "@dank-inc/use-get",
  "description": "A simple typscript react hook to get you that sweet sweet data from an endpoint.",
  "version": "0.2.0",
  "repository": {
    "url": "https://github.com/dank-inc/use-get"
  },
  "main": "dist/index.js",
  "author": "Elijah Lucian",
  "license": "ISC",
  "files": [
    "dist"
  ],
  "scripts": {
    "lint": "tsc --noEmit",
    "audit": "npm audit --audit-level=\"moderate\"",
    "compile": "rm -rf dist && tsc",
    "hitit": "npm run compile && npm publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "peerDependencies": {
    "react": "^17.0.2"
  },
  "devDependencies": {
    "@types/react": "^17.0.3",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.3"
  }
}
